// Токены интерфейса для canvas (HUD, журнал). Те же значения — в theme.css.
export const SANS = '"Inter", "SF Pro Text", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif';
export const SERIF = '"Iowan Old Style", "Palatino Linotype", Palatino, "Book Antiqua", Georgia, serif';
export const MONO = '"SF Mono", "JetBrains Mono", "Cascadia Mono", ui-monospace, Menlo, Consolas, monospace';

export const INK = (a = 1) => `rgba(236,234,228,${a})`;
export const ACCENT = (a = 1) => `rgba(242,179,91,${a})`;
export const DANGER = (a = 1) => `rgba(229,72,77,${a})`;
