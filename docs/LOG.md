# LOG

Журнал сессий. Формат: дата — майлстоун — что сделано / что сломано / что дальше.

- 2026-09-24 — M0 (код) — Ревью доков: лидар при закрытых глазах игрок не видит (варианты в GDD), фикстуры = RawFrame, грейбокс перенесён в M1. Код M0: контракт, gaze/lid/zones/калибровка, 3 источника, оверлей, протоколы записи с автоотчётом, 40 тестов на синтетике. Смоук в Chromium с фейковой камерой (тестовый портрет MediaPipe): лицо, LOST на чёрных кадрах, BLINK→CLOSED на закрашенных веках, мастер калибровки, запись и Replay работают без ошибок.
  / Сломано: известного нет. Все пороги — дефолты, не замеры. Софтверный WebGL в контейнере даёт 5 Гц и 200 мс инференса — это не показатель железа.
  / Дальше: приёмка M0 с камерой — Tracker → Калибровка → протоколы calm, blinks, zones, closed, lost → JSON в tests/fixtures/ → `npm test`.
